{
 "nbformat": 4,
 "nbformat_minor": 0,
 "metadata": {
  "colab": {
   "provenance": [],
   "gpuType": "T4",
   "name": "Cinematic Engine V17 — One-Click Setup"
  },
  "kernelspec": {
   "name": "python3",
   "display_name": "Python 3"
  },
  "accelerator": "GPU"
 },
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# 🎬 Cinematic Engine V17 — Enterprise AI Platform\n",
    "\n",
    "**One-click setup for Google Colab T4 GPU**\n",
    "\n",
    "### Quick Start\n",
    "1. `Runtime → Change runtime type → T4 GPU`\n",
    "2. Run **Cell 1** (Setup) — takes 3–5 min once\n",
    "3. Run **Cell 2** (Engine) — loads V16 engine\n",
    "4. Run **Cell 3** (Bridge) — starts WebSocket server\n",
    "5. Open `cinematic_engine_v17_2_final.html` in your browser\n",
    "6. Click **WS: OFF** in the Dashboard header to connect\n",
    "\n",
    "> **Note:** If accessing from outside Colab, set `USE_NGROK = True` in Cell 3\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ╔══════════════════════════════════════════════════════════╗\n",
    "# ║  CELL 1 — SETUP  (run once per Colab session)           ║\n",
    "# ╚══════════════════════════════════════════════════════════╝\n",
    "\n",
    "import subprocess, os, sys, torch\n",
    "\n",
    "# ── GPU Check ────────────────────────────────────────────────\n",
    "if not torch.cuda.is_available():\n",
    "    raise RuntimeError(\n",
    "        '\\n╔══════════════════════════════════════════╗'\n",
    "        '\\n║  NO GPU — Runtime → Change → T4 GPU     ║'\n",
    "        '\\n╚══════════════════════════════════════════╝'\n",
    "    )\n",
    "gpu  = torch.cuda.get_device_name(0)\n",
    "vram = torch.cuda.get_device_properties(0).total_memory / 1e9\n",
    "print(f'✓ GPU: {gpu} | VRAM: {vram:.1f} GB')\n",
    "print(f'✓ Recommended: {\"FLUX DEV\" if vram>=20 else \"FLUX SCHNELL\" if vram>=14 else \"SDXL\"}')\n",
    "\n",
    "# ── Install packages ─────────────────────────────────────────\n",
    "def run(cmd):\n",
    "    r = subprocess.run(cmd, shell=True, capture_output=True, text=True)\n",
    "    if r.returncode != 0 and r.stderr.strip():\n",
    "        print(f'  ⚠  {r.stderr.strip()[:120]}')\n",
    "    return r.returncode\n",
    "\n",
    "pkgs = [\n",
    "    ('pip install -q diffusers transformers accelerate xformers triton einops', 'diffusers'),\n",
    "    ('pip install -q gfpgan realesrgan tqdm sentencepiece nltk face_recognition', 'GFPGAN'),\n",
    "    ('pip install -q controlnet-aux open-clip-torch', 'ControlNet'),\n",
    "    ('pip install -q \"gradio>=4.0.0\"', 'Gradio'),\n",
    "    ('pip install -q bitsandbytes ip-adapter Pillow', 'IP-Adapter'),\n",
    "    ('pip install -q insightface onnxruntime-gpu', 'FaceID'),\n",
    "    ('pip install -q websockets', 'WebSocket bridge'),\n",
    "    ('pip install -q pyngrok', 'ngrok tunnel'),\n",
    "]\n",
    "for cmd, label in pkgs:\n",
    "    print(f'  Installing {label}…', end=' ')\n",
    "    run(cmd)\n",
    "    print('✓')\n",
    "\n",
    "# ── Download models ─────────────────────────────────────────\n",
    "os.makedirs('ip_adapter_sdxl/image_encoder', exist_ok=True)\n",
    "downloads = {\n",
    "    'GFPGANv1.3.pth': 'https://github.com/TencentARC/GFPGAN/releases/download/v1.3.0/GFPGANv1.3.pth',\n",
    "    'realesr-general-x4v3.pth': 'https://github.com/xinntao/Real-ESRGAN/releases/download/v0.2.5.0/realesr-general-x4v3.pth',\n",
    "    'ip_adapter_sdxl.bin': 'https://huggingface.co/h94/IP-Adapter/resolve/main/sdxl_models/ip-adapter_sdxl.bin',\n",
    "}\n",
    "for fname, url in downloads.items():\n",
    "    if not os.path.exists(fname):\n",
    "        print(f'  Downloading {fname}…', end=' ')\n",
    "        run(f'wget -q -O {fname} {url}')\n",
    "        print(f'✓ ({os.path.getsize(fname)/1e6:.0f}MB)')\n",
    "    else:\n",
    "        print(f'  ↷ {fname} already exists')\n",
    "\n",
    "# ── NLTK ─────────────────────────────────────────────────────\n",
    "import nltk\n",
    "nltk.download('punkt', quiet=True)\n",
    "nltk.download('punkt_tab', quiet=True)\n",
    "\n",
    "print('\\n' + '═'*52)\n",
    "print('  ✓ SETUP COMPLETE — ready to run Cell 2')\n",
    "print('═'*52)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ╔══════════════════════════════════════════════════════════╗\n",
    "# ║  CELL 2 — LOAD V16 ENGINE                               ║\n",
    "# ╚══════════════════════════════════════════════════════════╝\n",
    "# Upload cinematic_engine_v16_pro.py to Colab files first\n",
    "\n",
    "import importlib.util, sys, os\n",
    "\n",
    "ENGINE_PATH = 'cinematic_engine_v16_pro.py'   # adjust if needed\n",
    "\n",
    "if not os.path.exists(ENGINE_PATH):\n",
    "    raise FileNotFoundError(\n",
    "        f'Upload {ENGINE_PATH} to Colab first (Files panel on the left)'\n",
    "    )\n",
    "\n",
    "# Load V16 as a module\n",
    "spec   = importlib.util.spec_from_file_location('cev16', ENGINE_PATH)\n",
    "cev16  = importlib.util.module_from_spec(spec)\n",
    "sys.modules['__main__'].__dict__.update(\n",
    "    {k: v for k, v in cev16.__dict__.items() if not k.startswith('_')}\n",
    ")\n",
    "\n",
    "# Instantiate engine\n",
    "print('[ENGINE] Initialising CinematicEngineV16…')\n",
    "engine = CinematicEngineV16()\n",
    "print('[ENGINE] ✓ Ready — models not loaded yet')\n",
    "print('[ENGINE] Use CELL 3 bridge or Dashboard to load models')"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ╔══════════════════════════════════════════════════════════╗\n",
    "# ║  CELL 3 — START WEBSOCKET BRIDGE                        ║\n",
    "# ╚══════════════════════════════════════════════════════════╝\n",
    "\n",
    "# ── Config ───────────────────────────────────────────────────\n",
    "USE_NGROK = False   # Set True if accessing Dashboard from outside Colab\n",
    "                    # Requires free ngrok account at https://ngrok.com\n",
    "\n",
    "# If USE_NGROK=True, set your auth token:\n",
    "NGROK_TOKEN = ''    # Get from https://dashboard.ngrok.com/auth\n",
    "\n",
    "# ── Load bridge ──────────────────────────────────────────────\n",
    "import importlib.util, sys, os\n",
    "\n",
    "BRIDGE_PATH = 'cev17_backend.py'   # upload this file to Colab\n",
    "\n",
    "if not os.path.exists(BRIDGE_PATH):\n",
    "    raise FileNotFoundError(f'Upload {BRIDGE_PATH} to Colab first')\n",
    "\n",
    "spec   = importlib.util.spec_from_file_location('bridge', BRIDGE_PATH)\n",
    "bridge = importlib.util.module_from_spec(spec)\n",
    "spec.loader.exec_module(bridge)\n",
    "\n",
    "# ── ngrok setup ──────────────────────────────────────────────\n",
    "if USE_NGROK and NGROK_TOKEN:\n",
    "    from pyngrok import ngrok, conf\n",
    "    conf.get_default().auth_token = NGROK_TOKEN\n",
    "\n",
    "# ── Run ──────────────────────────────────────────────────────\n",
    "print('Starting WebSocket Bridge…')\n",
    "print('Dashboard → WS_URL should be: ws://localhost:7860/ws')\n",
    "if USE_NGROK:\n",
    "    print('Or the ngrok public URL shown below')\n",
    "print()\n",
    "\n",
    "bridge.run_bridge(\n",
    "    engine=engine,      # pass None here to test without engine\n",
    "    use_ngrok=USE_NGROK\n",
    ")\n",
    "\n",
    "# ── This cell runs forever — interrupt to stop ────────────────"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ╔══════════════════════════════════════════════════════════╗\n",
    "# ║  CELL 4 — OPTIONAL: Direct Python API usage             ║\n",
    "# ║  (if you want to generate without the Dashboard)        ║\n",
    "# ╚══════════════════════════════════════════════════════════╝\n",
    "\n",
    "# Load FLUX SCHNELL model\n",
    "engine.load_models(model_mode=ModelMode.FLUX_SCHNELL)\n",
    "\n",
    "# Generate a single scene\n",
    "cfg = GenerationConfig(\n",
    "    scene_text = 'A weary detective stands in a rain-soaked alley, neon lights reflecting in puddles',\n",
    "    camera     = 'Low Angle',\n",
    "    lighting   = 'Neon Cyberpunk',\n",
    "    style      = 'Cinematic',\n",
    ")\n",
    "img, meta = engine.generate_scene(cfg)\n",
    "print(f'Generated: {meta}')\n",
    "img  # display in notebook"
   ]
  }
 ]
}
